// Shared build settings. Debug.xcconfig and Release.xcconfig include this file.

// Default is empty so the project always compiles. Copy
// Secrets.sample.xcconfig -> Secrets.xcconfig to supply a real value; the
// optional include below overrides this default when that file exists.
GITHUB_CLIENT_ID =

#include? "Secrets.xcconfig"
